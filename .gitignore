.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/
tmp/
dedx

# Python packaging / test artifacts
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# gap-correction scratch (reproducible run dirs / sweeps / WDM tables / plots)
data/sweep/
data/wdm/
data/*_k[0-9]*/
data/*/*_k[0-9]*.png
data/*/wdm_*.png
data/*/dedx_kgap_sweep.png
dedx_g
tw[0-9]*_g[0-9]*.dat

# nuclear-stopping scratch (reproducible: regenerate with nuc_test.py / nuc_*.py)
dedx_nuc.dat
data/*/dedx_nuc.dat
dedx_nuc.pdf
data/*/dedx_nuc.pdf
# ...but keep the small committed test fixture (the bare rule above is for run dirs)
!tests/data/sample/dedx_nuc.dat

# editor / office lock and temp files
~$*
*.swp
*~

# ad-hoc run-output dirs in the repo root (committed material data lives under data/;
#  run scratch jobs under tmp/ or add the dir here). Each holds dedx.dat + AA *.den/.pot.
/ColdAl/
/WarmH2O/
/BH_5050/
/SiO2pstar/
/MylarWDM/
/AlphaDT_*/
/Deg_*/
/Vtot*/
/SmokeAl/
